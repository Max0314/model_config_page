# Workflow

## 开发步骤

1. 从 `main` 创建任务分支。
2. 确认是否影响接口契约。
3. 修改页面、样式或文档。
4. 运行 `make check` 或静态文件等价检查。
5. 提交并推送任务分支。

## 接入规则

- 宿主系统实现 `api-contract.md`。
- 页面保持可直接打开预览。
- 真实 API Key 和 secret 只能通过宿主后端保存。

## 部署与运行验证

- 本项目通常由宿主系统集成部署；发布、重载和线上验证由 Codex 通过 SSH 到宿主服务器执行。
- 部署后确认静态资源加载、接口契约兼容和浏览器控制台状态。
